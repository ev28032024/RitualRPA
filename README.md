# Discord RPA Automation with AdsPower

Автоматизация выполнения Discord команд `/bless` и `/curse` на нескольких аккаунтах через AdsPower.

**Использует Patchright** - антидетект форк Playwright с встроенной защитой от обнаружения.

## 🎯 Возможности

- ✅ Автоматическое выполнение команд `/bless` и `/curse`
- ✅ Работа с несколькими аккаунтами паровозиком (каждый благословляет следующего)
- ✅ Интеграция с AdsPower для управления профилями браузера
- ✅ **Встроенный антидетект Patchright** (не обнаруживается Cloudflare, DataDome)
- ✅ **Автоматическое скрытие navigator.webdriver**
- ✅ Человекоподобное поведение: случайные задержки при печати
- ✅ **Валидация конфигурации** - автоматическая проверка всех настроек
- ✅ **Продвинутое логирование** - детальные логи в файлы с timestamp
- ✅ **Надежная обработка ошибок** - гарантированное закрытие браузеров
- ✅ Async/await архитектура

## 📋 Требования

- Python 3.8+
- AdsPower установлен и запущен
- Аккаунты Discord уже залогинены в профилях AdsPower

## 🚀 Установка

1. **Установите зависимости:**
```bash
pip install -r requirements.txt
```

2. **Установите браузер Patchright:**
```bash
patchright install chrome
```

3. **Настройте AdsPower:**
   - Запустите AdsPower
   - Создайте профили для каждого аккаунта Discord
   - Залогиньтесь в Discord в каждом профиле
   - Запишите ID или номер профиля:
     - **ID профиля** — буквенно-цифровой идентификатор (например: `jxxxxxx`)
     - **Номер профиля** — числовой номер из колонки # (например: `1`, `2`, `3`)

4. **Настройте config.json:**
   - Скопируйте `config.example.json` в `config.json`
   - Откройте `config.json`
   - Замените `YOUR_SERVER_ID/YOUR_CHANNEL_ID` на реальные ID из URL Discord
   - Укажите `adspower_id` — ID профиля или его номер (система сама определит тип)
   - Замените `user1`, `user2` на Discord имена пользователей
   - Добавьте или удалите аккаунты по необходимости
   
   > ⚠️ **Валидация**: Скрипт автоматически проверит конфигурацию при запуске и выдаст понятные ошибки если что-то не так

## ⚙️ Конфигурация

### config.json структура:

```json
{
  "adspower_api_url": "http://localhost:50325",
  "discord_channel_url": "https://discord.com/channels/...",
  "delay_between_accounts": 5,
  "delay_between_commands": 3,
  "accounts": [
    {
      "name": "Account 1",
      "adspower_id": "jxxxxxxx",
      "discord_username": "username1"
    },
    {
      "name": "Account 2",
      "adspower_id": "2",
      "discord_username": "username2"
    }
  ]
}
```

### Параметры:

- **adspower_api_url**: URL локального API AdsPower (обычно не нужно менять)
- **discord_channel_url**: Ссылка на канал Discord
- **delay_between_accounts**: Задержка между обработкой аккаунтов (секунды)
- **delay_between_commands**: Задержка между командами /bless и /curse (секунды)
- **accounts**: Список аккаунтов

### Для каждого аккаунта:

- **name**: Понятное имя (для логов)
- **adspower_id**: ID профиля или номер профиля в AdsPower (автоопределение)
- **discord_username**: Discord имя пользователя (для команд)

> 💡 **Автоопределение типа идентификатора**: Система автоматически определяет тип `adspower_id`:
> - Если значение состоит только из цифр (например `"2"`, `"15"`) — используется как **номер профиля** (serial_number)
> - Если значение содержит буквы (например `"jxxxxxxx"`) — используется как **ID профиля** (user_id)

## 🎮 Использование

1. **Убедитесь что AdsPower запущен**

2. **Запустите скрипт:**
```bash
python main.py
```

3. **Скрипт выполнит:**
   - Проверку подключения к AdsPower
   - Для каждого аккаунта:
     - Запуск браузера
     - Переход в Discord канал
     - Выполнение `/bless` на следующем аккаунте
     - Выполнение `/curse` на следующем аккаунте
     - Закрытие браузера
   - Сохранение лога выполнения

## 📊 Логи

### Execution Logs (JSON)
После выполнения создается файл `execution_log_YYYYMMDD_HHMMSS.json` с детальной информацией о каждом действии:

```json
[
  {
    "timestamp": "2025-12-10T20:00:00",
    "account": "Account 1",
    "action": "bless",
    "success": true,
    "message": "Target: username2"
  }
]
```

### Application Logs
Детальные логи приложения сохраняются в директории `logs/`:
- `logs/ritual_rpa_YYYYMMDD_HHMMSS.log` - полный лог выполнения
- Содержит информацию об ошибках, предупреждениях и отладочную информацию
- Автоматически создается при каждом запуске

## 🔧 Как работает паровозик

Аккаунты обрабатываются по цепочке:
- Account 1 → благословляет/проклинает → Account 2
- Account 2 → благословляет/проклинает → Account 3
- Account 3 → благословляет/проклинает → Account 1 (замыкается)

## ⚠️ Важные замечания

1. **Discord Rate Limits**: Discord может ограничить частоту команд. Используйте разумные задержки.

2. **Проверьте логины**: Убедитесь что все аккаунты залогинены в Discord в профилях AdsPower.

3. **Права доступа**: Убедитесь что у аккаунтов есть доступ к каналу и право использовать команды.

4. **Имена пользователей**: Discord username должен быть точным (с учетом регистра).

## 🐛 Решение проблем

### AdsPower не подключается
- Проверьте что AdsPower запущен
- Проверьте URL API в config.json
- Порт по умолчанию: 50325

### Браузер не запускается
- Проверьте правильность `adspower_id` (ID профиля или его номер)
- Убедитесь что профиль существует в AdsPower
- При использовании номера профиля убедитесь что это положительное число

### Команды не выполняются
- Проверьте что аккаунт залогинен в Discord
- Проверьте права доступа к каналу
- Проверьте правильность Discord usernames
- Увеличьте задержки в config.json

### Autocomplete не появляется
- Увеличьте задержки между действиями
- Проверьте что канал поддерживает slash команды

## 📝 Доступные команды

Скрипт поддерживает следующие Discord команды:

- `/bless <user>` - дать благословение
- `/curse <user>` - наложить проклятие
- `/stats` - просмотр статистики (можно добавить в скрипт)
- `/journey` - получение роли Ascendant (можно добавить в скрипт)

## 🔄 Расширение функционала

Чтобы добавить выполнение `/stats` или `/journey`:

1. Откройте `main.py`
2. Добавьте вызов после команд bless/curse:

```python
# Execute /stats
discord.execute_stats()
time.sleep(delay_between_commands)

# Execute /journey
discord.execute_journey()
```

## 📞 Поддержка

При возникновении проблем:
1. Проверьте логи в консоли
2. Проверьте `logs/ritual_rpa_*.log` для детальной информации
3. Проверьте `execution_log_*.json` для истории действий
4. Убедитесь что все настройки корректны (скрипт автоматически валидирует конфигурацию)

## 🔄 История изменений

### v2.0.0 (12 декабря 2025) - Рефакторинг
- ✅ Исправлены критические баги в автоматизации
- ✅ Добавлена валидация конфигурации
- ✅ Внедрена система логирования
- ✅ Улучшена обработка ошибок
- ✅ Добавлена полная типизация
- ✅ Расширена документация

Подробнее: см. [REFACTORING_REPORT.md](REFACTORING_REPORT.md)

---

**Версия:** 2.0.0 (Refactored)  
**Статус:** Production Ready ✅
